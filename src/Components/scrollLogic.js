import React, { useState } from 'react'

export default function scrollLogic(){
  let scrollList = []

  for(let i; i < 10; i++){
    scrollList.push('')
  }

  scrollList.map((element, key)=>{
    return <li> oops</li>
  })

}